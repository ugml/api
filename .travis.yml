language: node_js

node_js:
  - "6"
  - "8"
  - "10"

env:
  - NODE_ENV=development

addons:
  mariadb: '10.4'

services:
  - redis-server

before_script:
  - npm run-script lint
  - npm run-script build
  - mysql -u root --password="root" < ci.sql
script:
  - npm run-script test
