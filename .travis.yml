language: node_js

node_js:
  - "6"
  - "8"
  - "10"

env:
  - NODE_ENV=development

addons:
  mariadb: '10.4'

services:
  - redis-server

before_script:
  - npm run-script lint
  - npm run-script build
script:
  - mysql -u $DB_USER --password=$DB_PASS < ci.sql
  - npm run-script test
